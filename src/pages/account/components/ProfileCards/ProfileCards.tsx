import React from 'react';

import styles from './ProfileCards.module.scss';

const ProfileCards = () => {
  return <section className={styles.cardsContainer}>ProfileCards</section>;
};

export default ProfileCards;
